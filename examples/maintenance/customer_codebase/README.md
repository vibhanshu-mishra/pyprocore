# Fake Customer Codebase

This local fixture documents `procore-sdk rfis --project 123` and
`procore-sdk analytics rfi-aging exports/rfis.json`.

It exists only for scanner examples and is never executed.
